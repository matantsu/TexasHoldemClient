<UserControl x:Class="TexasHoldemClient.uc.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TexasHoldemClient.uc"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="150">
    <Grid Name="Root">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="1"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding Name}"/>
            <TextBlock Grid.Row="1" Text="{Binding Money, StringFormat={}${0}}"/>
            <TextBlock Grid.Row="2" Text="{Binding Status}"/>
        </Grid>
        <Image Name="DealerImage" Source="img/dealer.jpg" HorizontalAlignment="Left" Height="20" VerticalAlignment="Top" Width="20" Margin="30,30,0,0"/>
    </Grid>
</UserControl>
