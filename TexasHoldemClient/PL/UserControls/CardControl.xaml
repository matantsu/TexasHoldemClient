<UserControl x:Class="TexasHoldemClient.PL.UserControls.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TexasHoldemClient.PL.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="96" d:DesignWidth="72">
    <Grid>
        <DockPanel Margin="4" x:Name="PhotoSpace">
            <Image Source="{Binding ImageSource}" />
        </DockPanel>
        <!--Border  Margin="4" BorderBrush="White" Background="LightGray" BorderThickness="1,1,1,1" CornerRadius="8,8,8,8">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Path=number}" Height="Auto" Width="72"/>
                <TextBlock Text="{Binding Path=type}" Height="Auto" Width="72"/>
            </StackPanel>
        </Border-->
        

    </Grid>
</UserControl>
